export const BANNER_CACHE_NAMESPACE = "banner"

// The one-image-per-banner cap lives in image.constant.ts (MAX_BANNER_IMAGES),
// next to the product gallery cap it mirrors — the check runs in
// image.service.ts, and cross-module imports are forbidden (ARCHITECTURE.md §5).
